# Taxi backend environment variables (example)
# Do NOT commit real secrets. Copy to .env and set real values.

# Postgres connection string (from database container)
DATABASE_URL=postgresql://appuser:dbuser123@localhost:5000/myapp

# JWT settings
JWT_SECRET_KEY=change_me_to_a_long_random_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
